# Linear OAuth + webhook credentials.
# Set in deployed environments via:
#   forge variables set --encrypt -e <env> <NAME> <value>
# This file is for local-development reference only - never commit real values.

LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=
LINEAR_WEBHOOK_SECRET=
